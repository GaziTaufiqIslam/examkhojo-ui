# Examkhojo UI Development

#### After cloning/downloading run
`npm install`

#### For normal dev with liveserver run 
`npm start`

#### For prodcution run the following
`npm run build` and serve from `dist` directory